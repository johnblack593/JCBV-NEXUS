# ═══════════════════════════════════════════════════════════════
# NEXUS v5.0 (beta) — Production Dependencies
# Python 3.11+ (Docker) | 3.13 (local dev)
# ═══════════════════════════════════════════════════════════════
# Installation:
#   pip install -r nexus/requirements.txt
# Scope: IQ Option primary venue. Bitget added in Phase 3 via ccxt.
# LLM providers: Groq + Gemini via direct aiohttp REST (no SDK wrappers).
# ML: scikit-learn ensemble only. TensorFlow is permanently removed.
# ═══════════════════════════════════════════════════════════════

# ── Exchange Connectivity ─────────────────────────────────────────
# IQ Option: JCBV Modernized Edition (zero busy-wait, TLS strict, async)
git+https://github.com/johnblack593/IQOP-API-JOHNBARZOLA.git@main

# ccxt: multi-exchange connectivity layer (Bitget, and future venues)
ccxt>=4.4.56

# WebSocket transport (used by IQ Option API and future real-time feeds)
websockets>=14.1

# ── Async HTTP (LLM calls, external APIs) ────────────────────────
# Direct REST client for Groq and Gemini APIs. Replaces all LangChain.
aiohttp>=3.9.5
aiohttp-retry>=2.8.3

# ── Data & Quantitative Analysis ─────────────────────────────────
pandas>=2.2.3
numpy>=2.2.1
scipy>=1.15.1

# Technical indicators (RSI, MACD, Bollinger, ATR, etc.)
ta>=0.11.0

# ── Machine Learning (lightweight ensemble, CPU-only) ────────────
# Powers the IQ Option signal confidence layer.
# NOTE: TensorFlow is permanently removed from NEXUS v5.0.
scikit-learn>=1.8.0

# ── Database & Persistence ───────────────────────────────────────
sqlalchemy>=2.0.0

# ── State Bus & Caching ──────────────────────────────────────────
redis>=5.2.0

# ── Observability ────────────────────────────────────────────────
prometheus-client>=0.21.0

# ── Reporting & Notifications ────────────────────────────────────
python-telegram-bot>=21.0

# ── Configuration ────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Serialization & Utilities ────────────────────────────────────
pydantic>=2.7.0
python-dateutil>=2.9.0
